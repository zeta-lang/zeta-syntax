module basic_sema @ 0.0.1

# comment

export
	push: int, (int | IntList) -> IntList
	\ a b =>
		if b is IntList as b
			then a, ^b
			else a, (b, null)

export
	pop: IntList -> int, ? ^IntList
	\ l => l.0, l.1

export usertype
	IntList: int, ? ^IntList